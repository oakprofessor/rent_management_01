<div id="page-inner">
  <div class="row">
    <div class="col-lg-12">
    <div class="panel panel-primary">
      <div class="panel-heading"><i class="fa fa-list"></i> DANH SÁCH TÀI KHOẢN</div>
      <div class="panel-body">
        <table class="table table-bordered">
          <thead>
            <tr>
              <th>Name</th>
              <th>Area</th>
              <th>Number of rooms</th>
              <th>Image</th>
              <th>State</th>
              <th>Tác vụ</th>
            </tr>
          </thead>
          <tbody>

            <%= paginate @rooms, theme: "twitter-bootstrap-3" %>
            <%= render partial: "room",  collection: @rooms, as: "room" %>

          </tbody>
        </table>
      </div>
    </div>
    <button class="btn btn-success"><i class="fa fa-plus"><%= link_to "ĐẶT PHÒNG", controller: "homes", action: "new_room", id: @home.id %></i></button>
    </div>
  </div>
  <hr />
</div>
