<%= form_for(admin_home, local:true) do |form| %>
  <% if admin_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_home.errors.count, "error") %> prohibited this admin_home from being saved:</h2>

      <ul>
      <% admin_home.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :room_number %>
    <%= form.number_field :room_number %>
  </div>

  <div class="field">
    <%= form.label :room_empty %>
    <%= form.number_field :room_empty %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
