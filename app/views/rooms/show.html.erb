<div class="content" stt="room" id="room">
  <div class="container">
    <div class="row details">
      <div class="col-sm-12 well">
        <div class="header_detail">
          <%= t ".show_title" %>
        </div>
        <div class="row">
          <div class="col-sm-4" >
            <div class="imgPost">
              <a href="#"><%= image_tag "imgHouse/post1.jpg" %></a>
            </div>
          </div>
          <div class="col-sm-8 ">
            <div class="well">
              <div class="labelPost">
                <%= form_for :room do |f| %>
                  <%= f.label t(".show_a") %>
                  <%= f.label t(".show_b") %>
                  <%= f.label t(".show_c") %>
                  <%= f.label t(".show_d") %>
                  <%= f.label t(".show_e") %>
                <% end %>
              </div>
              <div class="contentPost">
                <div class="list-product">
                  <div class="row">
                    <%= @room.name%>
                  </div>
                  <div class="row">
                    <% @home = Home.find_by id: @room.id %>
                    <%= @home.address %>
                  </div>
                  <div class="row">
                    <%= @room.number_room %>
                  </div>
                  <div class="row">
                    <%= @room.number_room %>
                  </div>
                  <div class="row">
                    <%= @room.price %>
                  </div>
                </div>
              </div>
              <button class="btn submit"><span class="glyphicon glyphicon-check"></span><%= link_to t(".show_set"), controller: "rooms", action: "bookroom", id: @room.id %></button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
